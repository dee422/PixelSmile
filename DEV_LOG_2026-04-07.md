# 开发日志（2026-04-07）

## 今日完成

1. 项目功能扩展
- 重构并保留 `PixelSmileConditioning`。
- 新增节点：
  - `PixelSmileExpressionLibraryLoad`
  - `PixelSmileExpressionPromptFromLibrary`
  - `PixelSmileExpressionPromptByIndex`
  - `PixelSmileExpressionNames`

2. 本地表情库工具链
- 新增脚本：`scripts/build_expression_library.py`
  - 支持 `--label-source ollama`（视觉模型打标）
  - 支持 `--label-source filename`（无 Ollama 兜底）
- 新增脚本：`scripts/normalize_expression_library.py`
  - 支持 aliases 合并同义词
- 新增模板：
  - `expression_library/library.template.json`
  - `expression_library/aliases.template.json`

3. 批处理与验证
- 新增一键测试：`run_expression_library_test.bat`
- 已在本地验证：可生成并标准化
  - `expression_library/library.demo.json`
  - `expression_library/library.demo.normalized.json`

4. ComfyUI 集成排障
- 已确认 custom_nodes 符号链接正确。
- 已确认插件可被 ComfyUI 正常导入。
- 已定位并解决多项运行问题：
  - 端口占用（8188）
  - 缺失节点包（`ComfyUI-GGUF`）
  - GGUF 文件损坏（重下后修复）
  - 资源路径缺失（UNet/LoRA/VAE/输入图）
  - CLIP 架构不匹配（需改为 `Load CLIP + qwen_2.5_vl_7b_fp8_scaled.safetensors`）

5. 环境约定
- 后续统一使用：
  - `C:\Users\deejo\anaconda3\envs\tts\python.exe`（Python 3.10）

## 当前状态

- 模型文件基本到位（clip / unet / lora / vae）。
- 工作流已可加载并进入调参阶段。
- 目前建议的稳定链路：
  - 使用 `Load CLIP`（非 GGUF）读取 `qwen_2.5_vl_7b_fp8_scaled.safetensors`
  - 保持 Qwen Edit 的 UNet/LoRA/VAE 组合

## 明日计划（第3阶段：独立界面）

目标：做一个不需要用户进入 ComfyUI 的本地 Web 界面 MVP（调用 ComfyUI API）。

1. 新增 `app.py`（Gradio）
- 上传图片
- 选择表情
- 调整 `score/method/分辨率/seed`
- 触发生成并预览

2. 新增 `comfy_client.py`
- 封装 `/prompt`、`/history`、`/view`
- 支持轮询任务状态和错误回传

3. 接入表情库
- 读取 `expression_library/library.normalized.json`
- 自动填充表情下拉列表

4. 新增启动脚本
- 用 `tts` 环境一键启动 UI

## 备注

- 若继续出现比例问题，优先统一 `ImageScale` 与 `EmptyLatent` 的宽高比例（例如 768x1152）。
- 若再出现 shape mismatch，先核对 CLIP 节点类型与 text encoder 文件是否匹配。

## 追加进度（独立界面 MVP）

时间：2026-04-07（晚间）

### 已落地文件

- `comfy_client.py`
  - 封装 ComfyUI API：`/upload/image`、`/prompt`、`/history/{prompt_id}`、`/view`
- `app.py`
  - Gradio MVP：上传图片、选择表情、调 `score/method/strength/width/height/seed`、生成并展示结果
- `config/ui_config.template.json`
- `config/ui_config.json`
- `run_pixelsmile_ui.bat`
  - 固定使用 `C:\Users\deejo\anaconda3\envs\tts\python.exe`
- `workflow/README_API_WORKFLOW.md`
  - 说明必须导出 API 格式 workflow

### 已验证

- `app.py` / `comfy_client.py` 语法编译通过（Python 3.10 tts）
- `gradio`、`requests`、`Pillow` 可导入

### 当前阻塞点

- 还未提供 `workflow/qwen_image_edit_api.json`（API 格式工作流）
- 需在 ComfyUI 里先导出 API workflow，才能从 UI 一键提交生成

### 重启后第一步

1. 打开 ComfyUI，导出 API workflow 到：
   - `workflow/qwen_image_edit_api.json`
2. 运行：
   - `run_pixelsmile_ui.bat`
3. 打开：
   - `http://127.0.0.1:7861`

### 下一步开发计划

- 自动校验 `ui_config.json` 的节点 ID 是否存在于 API workflow
- 增加“批量输出多表情”按钮（循环 expression）
- 增加运行日志与失败重试

## 追加进度（批量输出功能）

- 已重写 `app.py`（清理乱码并升级功能）。
- 新增按钮：`一键批量生成全部表情（保存到相册）`。
- 批量模式行为：
  - 按表情库顺序逐个生成；
  - 自动保存到 `album_dir`；
  - UI 返回批量预览画廊与完整日志。
- 单张模式保留：支持按开关决定是否保存相册。

## 追加进度（第一版收尾）

- 新增相册管理增强：
  - 收藏/取消收藏（写入相册下 `.favorites.json`）
  - 只看收藏筛选
  - 选中图片移动到其他相册
  - 按当前筛选批量重命名
- 已有能力完善：
  - 历史相册列表、分页、筛选、点击放大、重命名、删除
  - 相册名称（批次）生效，批量生成统一归档
  - 自定义表情：测试后保存入库
- `app.py` 语法与 UI 构建校验通过。
